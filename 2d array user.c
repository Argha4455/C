#include<stdio.h>
   void main()
   {
       int a[100][100],b[100][100],i,m,c,v;
       printf("Enter the number of row");
       scanf("%d",&i);
       printf("Enter the number of column");
       scanf("%d",&m);
       printf("Enter the elements of the array");
       for(c=0;c<m;c++){
        for(v=0;v<i;v++){
       scanf("%d",&a[m][i]);
          }
          printf("new coloumn");
   }
   printf("\r");
   }
